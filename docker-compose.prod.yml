services:
  web:
    container_name: ${WEB_CONTAINER_NAME:-initiare_erp-web}
    image: ${WEB_IMAGE:-initiare_erp-web:latest}
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_URL: /api
    restart: unless-stopped
    env_file:
      - .env
    environment:
      NODE_ENV: production
      HOST: ${HOST:-0.0.0.0}
      HOSTNAME: ${HOSTNAME:-0.0.0.0}
      PORT: ${WEB_PORT:-3000}
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.initiare_erp-web.rule=Host(`${TRAEFIK_HOST:?set_TRAEFIK_HOST}`) && !PathPrefix(`/api`)"
      - "traefik.http.routers.initiare_erp-web.entrypoints=websecure"
      - "traefik.http.routers.initiare_erp-web.tls=true"
      - "traefik.http.routers.initiare_erp-web.tls.certresolver=${TRAEFIK_CERTRESOLVER:-letsencrypt}"
      - "traefik.http.routers.initiare_erp-web.service=initiare_erp-web"
      - "traefik.http.services.initiare_erp-web.loadbalancer.server.port=${WEB_PORT:-3000}"
      - "traefik.http.routers.initiare_erp-web-http.rule=Host(`${TRAEFIK_HOST:?set_TRAEFIK_HOST}`) && !PathPrefix(`/api`)"
      - "traefik.http.routers.initiare_erp-web-http.entrypoints=web"
      - "traefik.http.routers.initiare_erp-web-http.service=initiare_erp-web"
      - "traefik.http.middlewares.initiare_erp-https-redirect.redirectscheme.scheme=https"
      - "traefik.http.middlewares.initiare_erp-web-sec.headers.stsSeconds=31536000"
      - "traefik.http.middlewares.initiare_erp-web-sec.headers.stsIncludeSubdomains=true"
      - "traefik.http.middlewares.initiare_erp-web-sec.headers.stsPreload=true"
      - "traefik.http.middlewares.initiare_erp-web-sec.headers.contentSecurityPolicy=upgrade-insecure-requests"
      - "traefik.http.middlewares.initiare_erp-web-sec.headers.contentTypeNosniff=true"
      - "traefik.http.middlewares.initiare_erp-web-sec.headers.browserXssFilter=true"
      - "traefik.http.middlewares.initiare_erp-web-sec.headers.frameDeny=true"
      - "traefik.http.routers.initiare_erp-web.middlewares=initiare_erp-web-sec"
      - "traefik.http.routers.initiare_erp-web-http.middlewares=initiare_erp-https-redirect,initiare_erp-web-sec"
      - "traefik.docker.network=${TRAEFIK_NETWORK:-proxy}"
    networks:
      - app
      - proxy
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "node -e \"fetch('http://127.0.0.1:${WEB_PORT:-3000}/login').then((r) => process.exit(r.ok ? 0 : 1)).catch(() => process.exit(1))\"",
        ]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 30s

  api:
    container_name: ${API_CONTAINER_NAME:-initiare_erp-api}
    image: ${API_IMAGE:-initiare_erp-api:latest}
    build:
      context: ./api
      dockerfile: Dockerfile
    restart: unless-stopped
    env_file:
      - .env
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: ${API_PORT:-4000}
      DATABASE_URL: ${DATABASE_URL:-postgresql://veridia:veridia@postgres:5432/initiare_erp?schema=public}
      JWT_SECRET: ${JWT_SECRET:?set_JWT_SECRET}
      APP_ORIGIN: ${APP_ORIGIN:?set_APP_ORIGIN}
      SEED_ADMIN_EMAIL: ${SEED_ADMIN_EMAIL:-admin@veridia.local}
      SEED_ADMIN_PASSWORD: ${SEED_ADMIN_PASSWORD:-ChangeMe123!}
      SEED_COMPANY_NAME: ${SEED_COMPANY_NAME:-Acme Holdings}
      SEED_COMPANY_DOMAIN: ${SEED_COMPANY_DOMAIN:?set_SEED_COMPANY_DOMAIN}
      LOG_LEVEL: ${API_LOG_LEVEL:-info}
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.initiare_erp-api.rule=Host(`${TRAEFIK_HOST:?set_TRAEFIK_HOST}`) && PathPrefix(`/api`)"
      - "traefik.http.routers.initiare_erp-api.priority=100"
      - "traefik.http.routers.initiare_erp-api.entrypoints=websecure"
      - "traefik.http.routers.initiare_erp-api.tls=true"
      - "traefik.http.routers.initiare_erp-api.tls.certresolver=${TRAEFIK_CERTRESOLVER:-letsencrypt}"
      - "traefik.http.routers.initiare_erp-api.service=initiare_erp-api"
      - "traefik.http.services.initiare_erp-api.loadbalancer.server.port=${API_PORT:-4000}"
      - "traefik.http.routers.initiare_erp-api-http.rule=Host(`${TRAEFIK_HOST:?set_TRAEFIK_HOST}`) && PathPrefix(`/api`)"
      - "traefik.http.routers.initiare_erp-api-http.priority=100"
      - "traefik.http.routers.initiare_erp-api-http.entrypoints=web"
      - "traefik.http.routers.initiare_erp-api-http.service=initiare_erp-api"
      - "traefik.http.middlewares.initiare_erp-api-sec.headers.contentTypeNosniff=true"
      - "traefik.http.middlewares.initiare_erp-api-sec.headers.frameDeny=true"
      - "traefik.http.routers.initiare_erp-api.middlewares=initiare_erp-api-sec"
      - "traefik.http.routers.initiare_erp-api-http.middlewares=initiare_erp-https-redirect,initiare_erp-api-sec"
      - "traefik.docker.network=${TRAEFIK_NETWORK:-proxy}"
    networks:
      - app
      - proxy
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "node -e \"fetch('http://127.0.0.1:${API_PORT:-4000}/api/health').then((r) => process.exit(r.ok ? 0 : 1)).catch(() => process.exit(1))\"",
        ]
      interval: 15s
      timeout: 5s
      retries: 10
      start_period: 30s

  postgres:
    container_name: ${DB_CONTAINER_NAME:-initiare_erp-postgres}
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-initiare_erp}
      POSTGRES_USER: ${POSTGRES_USER:-veridia}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-veridia}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - app
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-veridia} -d ${POSTGRES_DB:-initiare_erp}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s

volumes:
  postgres_data:

networks:
  app:
    driver: bridge
  proxy:
    external: true
    name: ${TRAEFIK_NETWORK:-proxy}

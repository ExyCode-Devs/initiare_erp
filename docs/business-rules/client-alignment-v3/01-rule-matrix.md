# Client Alignment v3 Rule Matrix

| Rule ID | Source section | Atomic rule | Current system evidence | Status | Risk | Validation note |
| --- | --- | --- | --- | --- | --- | --- |
| RULE-001 | 1, 3.1 | Product must support multi-tenant resale, not hardcoded to Initiare only. | `Company`, `User.companyId`, tenant-scoped queries in `api/prisma/schema.prisma`, auth payload includes `companyId` in `api/src/routes/auth.ts`. | MATCH | Medium | Keep `Company` as current tenant root unless broader platform layer is approved. |
| RULE-002 | 3.2 | Operational tables must carry tenant/company isolation. | Most operational tables in `api/prisma/schema.prisma` include `companyId`; examples: `FinancialDraft`, `ErpConnection`, `InboundEmail`, `AuditLog`. | PARTIAL_MATCH | High | Keep current tenant isolation baseline, then extend target-doc company scoping to all new operational entities. |
| RULE-003 | 3.3, 3.4 | Backend must enforce cross-tenant isolation on reads, writes, approvals, exports, jobs, and integrations. | `request.user.companyId` filters in `api/src/routes/financial-drafts.ts`, `api/src/routes/data.ts`, `api/src/routes/legal-entities.ts`, integration routes. | MATCH | High | Add explicit tests for wrong-tenant cases in future waves. |
| RULE-004 | 2.6 | Domain naming should distinguish tenant, tenant user, business client, payer, supplier. | Current schema uses `Company`, `User`, `Client`, `Supplier`, `LegalEntity`, `FinancialDraft`. | PARTIAL_MATCH | Medium | Keep legacy names only as migration context. New schema, docs, and UI copy should move to target vocabulary. |
| RULE-005 | 3.5 | Tenant must own its own connected emails, aliases, templates, plans, departments, integration credentials, schedules, and BI config. | Per-tenant OMIE/Asaas connections and legal entities exist in `api/prisma/schema.prisma`, `api/src/routes/omie.ts`, `api/src/routes/asaas.ts`; email/template/department/BI config models absent. | PARTIAL_MATCH | High | Reuse current tenant-scoped integrations, then add missing target-doc tenant-owned config domains in later waves. |
| RULE-006 | 3.6, 18.2 | Role model should include platform super-admin and future tenant client user levels. | Current `UserRole` only has `ADMIN`, `ANALYST`, `VIEWER` in `api/prisma/schema.prisma`. | CONFLICT_PARTIAL | Medium | Treat current roles as legacy MVP layer. Expand toward target role taxonomy when auth scope is implemented. |
| RULE-007 | 4.1, 4.2 | AI must never create definitive financial records directly without human approval. | Draft ingestion creates `FinancialDraft` first in `api/src/routes/ai-events.ts`; review flow in `api/src/routes/financial-drafts.ts`. | MATCH | High | Preserve this as hard guard. |
| RULE-008 | 4.2, 7.8, 20 | Initial version must require human approval before downstream OMIE/Asaas execution. | Approve route exists; OMIE export is separate manual action in `api/src/routes/financial-drafts.ts`; Asaas sync/webhook flows are not approval-driven. | PARTIAL_MATCH | High | Preserve human approval rule, then migrate all definitive provider execution paths behind approved pre-entry workflow. |
| RULE-009 | 4.2 | Mandatory flow is source captured, evidence saved, pre-entry created, user reviews, then external execution, then audit/logging. | Source event saved, AI run saved, draft saved in `api/src/lib/ai-draft-service.ts`; review/audit in `api/src/lib/draft-workflow.ts`; OMIE request logs in `ErpRequestLog`. | PARTIAL_MATCH | High | Current pieces exist, but they must be unified into target-doc intake -> pre-entry -> execution -> audit sequence. |
| RULE-010 | 3.1, 5.1 | Intake should support Gmail/Google Workspace operational email flow. | Current backend intentionally disables `/mailboxes` and `/inbox/emails` with `410` in `api/src/routes/mailboxes.ts` and `api/src/routes/inbox.ts`. | CONFLICT_TOTAL | High | Target doc wins. Current Active Actions-first path should be treated as legacy unless user later narrows scope. |
| RULE-011 | 5.1 | If email path exists, system should monitor operational inboxes, not each alias directly. | Current model stores `MailboxAccount`, but runtime routes are disabled; legal entities keep alias-like arrays in `defaultRecipientEmails` and `defaultMailboxIds`. | CONFLICT_PARTIAL | High | Restore and modernize operational inbox model toward target doc, not toward current disabled runtime. |
| RULE-012 | 5.1 | Alias, original recipient, and authorized sender should identify tenant business client in priority order. | Current routing uses `targetCnpj`, recipient alias, mailbox alias in `api/src/lib/legal-entities.ts`; no authorized sender model. | PARTIAL_MATCH | High | Keep current routing hints only as starting point. Implement target priority order with business-client and authorized-sender models. |
| RULE-013 | 5.1 | Unknown or unsafe routing must go to review, not definitive execution. | `resolveCompanyFromDraftRoute` returns unrouted results; signed event endpoint returns `422` when no company resolved in `api/src/routes/ai-events.ts`; draft routing statuses exist. | MATCH | High | Add explicit UI for ambiguous/unrouted queue later. |
| RULE-014 | 5.1 | System should preserve full email/thread metadata and original alias. | Current `InboundEmail` has headers/body/attachments fields in schema, but active runtime path no longer uses inbox ingestion. | CONFLICT_PARTIAL | Medium | Implement target metadata preservation as part of restored inbox path. |
| RULE-015 | 5.1 | Email statuses should cover needs_review, unauthorized_sender, pre_entry_created, executed, answered, duplicate. | Current `InboxStatus` enum has `RECEBIDO`, `PROCESSANDO`, `PROCESSADO`, `AGUARDANDO_VALIDACAO`, `APROVADO`, `REJEITADO`, `ERRO`. | PARTIAL_MATCH | Medium | Expand current inbox statuses toward target-doc operational lifecycle instead of reusing narrow legacy enum. |
| RULE-016 | 5.1 | Automatic replies should be tenant-configurable by flow and client. | No reply-template model or reply route found. | NET_NEW | Medium | Needs product call if email path stays. |
| RULE-017 | 5.1 | Initial processing frequency should likely be scheduled batches per tenant. | Current external ingress is push-based Active Actions event route; old worker poller exists in `api/src/worker.ts` but mailbox routes are disabled. | CONFLICT_PARTIAL | Medium | Default implementation direction should favor tenant-scheduled batch processing from target doc. |
| RULE-018 | 6 | Operational inbox screen should centralize inbound messages, type, confidence, responsible operator, and linked pre-entry. | Frontend still has `src/routes/inbox-financeiro.tsx`, but backend `/api/inbox/emails` returns `410`. | CONFLICT_TOTAL | High | Restore backend contract and align UI to target doc instead of removing inbox concept. |
| RULE-019 | 6 | Financial requests should create pre-entry payable or receivable, low-confidence items to review. | Signed event payload normalizes `draft.direction`; draft queue/review exists in `api/src/routes/financial-drafts.ts`. | MATCH | High | Keep as core contract. |
| RULE-020 | 7 | Faturamento/Aprovação screen is central approval layer for all financial entries. | `src/routes/validacao-financeira.tsx` and `/api/financial-drafts*` already implement review queue. | MATCH | High | Extend, do not replace. |
| RULE-021 | 7.3 | Pre-entries should support origins email, attachment, OMIE, Asaas, bank, external system, manual, recurring job. | Current drafts support source event, legacy email, AI run, extraction run; no first-class origin enum for all target sources. | PARTIAL_MATCH | Medium | Replace fragmented source fields with one target-doc origin model across all intake and sync paths. |
| RULE-022 | 7.5 | Pre-entry status machine should include edited, duplicated, sent_to_integration, integration_error, completed, answered. | `DraftStatus` only has `PENDENTE_REVISAO`, `APROVADO`, `REJEITADO`; extra routing/sync states live elsewhere. | CONFLICT_PARTIAL | High | Redesign status machine toward target doc, then migrate current draft states. |
| RULE-023 | 7.7 | User actions should include reprocess AI, mark duplicate, send to OMIE, generate boleto, invoice linkage, reply original email. | Current actions are edit, approve, reject, OMIE export in `api/src/routes/financial-drafts.ts`; no duplicate/reprocess/reply/boleto actions. | PARTIAL_MATCH | High | Keep current actions only as minimal baseline, then add missing target-doc operator actions through Waves 03 and 04. |
| RULE-024 | 7.8 | No initial auto-send to financial systems without human approval. | OMIE export requires approved draft and explicit POST in `api/src/lib/omie-export-service.ts`; tests reflect manual review flow. | MATCH | High | Keep. |
| RULE-025 | 8 | Payables flow should identify supplier, extract fields, approve, then create supplier and payable in OMIE. | Approval currently creates local `AccountPayable` in `api/src/lib/draft-workflow.ts`; OMIE creation is separate manual export in `api/src/lib/omie-export-service.ts`. | CONFLICT_TOTAL | High | Target doc wins. Approval flow should be redesigned so approved payable proceeds to definitive OMIE creation path. |
| RULE-026 | 8.5 | Supplier identity should use internal base first, then OMIE after approval, avoiding duplicates by document. | Current supplier upsert uses company scope and `cnpj` or `name` in `api/src/lib/draft-workflow.ts`; OMIE mapping exists for export. | PARTIAL_MATCH | Medium | Preserve document-based matching logic, then extend supplier model and approval-time OMIE sync toward target doc. |
| RULE-027 | 8.6 | Missing essential payable fields must keep item in review. | Drafts can exist with nullable `amount` and `dueDate`; OMIE export blocks missing required fields in `api/src/lib/omie-export-service.ts`. | PARTIAL_MATCH | High | Move required-field guard from export-only stage into target pre-entry approval workflow. |
| RULE-028 | 8.7, 18.12, 18.13 | Financial classification should be client-specific and support categories and departments/cost centers. | Draft has `suggestedCategory` and `finalCategory`; no first-class department or business-client-specific category model found. | PARTIAL_MATCH | Medium | Keep current category suggestion only as precursor, then introduce target business-client category and department models. |
| RULE-029 | 8.9, 9.10 | Payable and receivable duplicates should be first-class review states. | No draft duplicate action or duplicate enum found; some route dedupe exists for external event IDs only. | NET_NEW | High | Important safety gap. |
| RULE-030 | 9 | Receivables should also pass through pre-entry review before definitive downstream action. | Receivable drafts exist and can be approved into local `AccountReceivable` in `api/src/lib/draft-workflow.ts`; Asaas payments are visibility/sync oriented in `api/src/routes/asaas.ts`. | PARTIAL_MATCH | High | Migrate all receivable sources into one target pre-entry -> approval -> definitive execution flow. |
| RULE-031 | 9.8, 9.9 | Receivable approval path may need boleto generation and email send. | No boleto-generation or receivable email-send flow found in repo. | NET_NEW | High | Depends on OMIE/Asaas chosen source of truth. |
| RULE-032 | 10 | Contract recurring billing should create receivable pre-entries, then require approval before faturamento. | No contract sync or recurring billing module found. | NET_NEW | Medium | Add only after intake and pre-entry model stabilize. |
| RULE-033 | 11 | OMIE service orders should become receivable pre-entries pending approval. | No OS-specific route/model found. | NET_NEW | Medium | Requires OMIE API validation first. |
| RULE-034 | 12 | Daily reconciliation should cross bank movement with payables/receivables and surface exceptions. | Existing `ReconciliationItem` and `/api/reconciliation` in `api/src/routes/data.ts`; UI route exists for `conciliacao`. | PARTIAL_MATCH | Medium | Reuse current reconciliation surface, then evolve it into target daily movement import and exception workflow. |
| RULE-035 | 12.4 | Asaas fees should be linked to received charges and auto-classified when safe. | Asaas sync/webhook and fee fields exist in `api/src/routes/asaas.ts`, `api/src/lib/asaas-sync-service.ts`, but no reconciliation-level auto-classification workflow found. | PARTIAL_MATCH | Medium | Existing fee data is useful input, but target-doc fee linkage and classification still need dedicated reconciliation workflow. |
| RULE-036 | 13 | Asaas to OMIE sync should support customers, receivables, status, cancellations, exceptions, and webhook or job. | Asaas webhook and sync routes exist; OMIE catalog/export exists; no full bidirectional Asaas-to-OMIE receivable sync orchestration found. | PARTIAL_MATCH | High | Reuse current provider hooks, then implement full target-doc sync orchestration and exception handling. |
| RULE-037 | 13.7, 18.4 | One business client may map to multiple operational CNPJs with distribution rules. | `LegalEntity` exists, but no `business_client_entities` or distribution-rule model exists. | NET_NEW | High | High-impact domain addition. |
| RULE-038 | 13.8 | Monthly invoice cap per CNPJ should be configurable and may reroute or block emissions. | No cap or routing-threshold model found. | NET_NEW | Medium | Possibly temporary business rule, validate before schema work. |
| RULE-039 | 14 | Fiscal note issuance path must support Asaas, OMIE, or direct government API strategy. | No dedicated invoicing module found. | NEEDS_VALIDATION | High | Product decision first, implementation later. |
| RULE-040 | 15 | BI should be tenant-safe and eventually client-level with operational drill-down. | Dashboard and reports exist in `api/src/routes/dashboard.ts`, `api/src/routes/data.ts`, `src/routes/executivo.tsx`, but not client-scoped BI model from doc. | PARTIAL_MATCH | Medium | Current analytics are only seed material. Target-doc BI must be rebuilt on business-client and operational fact model. |
| RULE-041 | 16 | Operator daily view should aggregate approvals, alerts, faturamento due items, and exceptions. | Dashboard, exceptions, automations, and financial queue already exist across `src/routes/index.tsx`, `api/src/routes/dashboard.ts`, `src/lib/navigation.ts`. | PARTIAL_MATCH | Medium | Use current dashboard only as base, then extend it into target operator daily command view. |
| RULE-042 | 17 | Future customer portal should expose limited self-service data and requests. | No external client portal auth/model found. | NET_NEW | Medium | Later wave only. |
| RULE-043 | 18 | Suggested target model includes dedicated tables for business clients, aliases, authorized senders, pre-entry attachments, sync logs, jobs. | Current schema lacks dedicated alias/sender/job tables, but has nearby concepts: `Client`, `MailboxAccount`, `FinancialDraft`, `ProcessingJobRun`, `ErpSyncRecord`, `ErpRequestLog`. | PARTIAL_MATCH | High | Treat current schema as migration input only. Introduce target tables explicitly instead of stretching legacy models. |
| RULE-044 | 19 | Prioritized phases should start with auth, client registration, aliases/email setup, pre-entry base, inbox, approval screen, storage/logs. | Auth, review screen, drafts, audit, storage, integrations already exist; alias/business-client/Gmail setup do not. | PARTIAL_MATCH | Medium | Reorder remaining work to match target-doc phase sequence, using current assets only where they accelerate that path. |
| RULE-045 | 20 | Definitive financial actions must always log, encrypt credentials, keep traceability, and show API errors to operator. | `ErpRequestLog`, `AuditLog`, encrypted OMIE/Asaas secrets, surfaced sync history in review screen and settings. | MATCH | High | Strong current foundation. |
| RULE-046 | 21 | OMIE API capabilities, Asaas mode, clients in MVP, approval visual split, and job cadence are unresolved business inputs. | Unresolved in repo, mirrored by section 21 of source doc. | NEEDS_VALIDATION | High | Keep in `validation-backlog.md`. |

## Notes

- `CONFLICT_TOTAL` rows mean current repo is materially wrong against target doc and should be migrated.
- `CONFLICT_PARTIAL` rows mean keep current evidence for migration context, but implementation direction still follows target doc.
- `NET_NEW` rows are backlog candidates after core intake and approval model stabilize.
